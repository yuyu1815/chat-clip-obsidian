// Gemini communication layer placeholder
// This extension operates on-page and does not call Gemini APIs directly.
// Keep this module for future network-related needs (e.g., exporting via APIs).
export function sendRequest(/* payload */) {
  throw new Error('Gemini comm.sendRequest is not implemented in content script context');
}
